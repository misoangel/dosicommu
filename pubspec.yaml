name: dosicommu
description: 부산광역시 사하구의회 도시위원회 업무 관리 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.2
  firebase_messaging: ^15.1.3
  # UI
  cupertino_icons: ^1.0.8
  go_router: ^14.3.0
  # 파일
  file_picker: ^8.1.2
  # 날짜
  intl: ^0.19.0
  # 기타
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
